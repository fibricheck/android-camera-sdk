name: Prepare Release

on:
  workflow_dispatch:
    inputs:
      version:
        description: "Version to release (e.g. 1.2.0)"
        required: true
        type: string
      target_branch:
        description: "Branch to open the PR against (e.g. main, v1.2.3)"
        required: false
        default: "main"
        type: string

permissions:
  contents: write
  pull-requests: write

jobs:
  prepare:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ inputs.target_branch }}

      - name: Configure git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Generate changelog
        uses: orhun/git-cliff-action@v4
        with:
          config: cliff.toml
          args: --tag v${{ inputs.version }} --output CHANGELOG.md

      - name: Update sdk-release.json
        run: |
          jq --arg version "${{ inputs.version }}" \
             --arg date "$(date -u +%Y-%m-%d)" \
             '.version = $version | .releaseDate = $date' \
             sdk-release.json > sdk-release.json.tmp
          mv sdk-release.json.tmp sdk-release.json

      - name: Create release branch and PR
        run: |
          BRANCH="chore/release-v${{ inputs.version }}-$(date -u +%Y-%m-%d)"
          git checkout -b "$BRANCH"
          git add CHANGELOG.md sdk-release.json
          git commit -m "chore: Release v${{ inputs.version }}"
          git push origin "$BRANCH"

          {
            echo "Updates the changelog and release metadata for **v${{ inputs.version }}**. After merging, you can trigger the release manually via the Deploy workflow."
            echo ""
            git cliff --tag v${{ inputs.version }} --unreleased --strip all
          } > pr_body.md

          gh pr create \
            --title "chore(release): v${{ inputs.version }}" \
            --body-file pr_body.md \
            --base "${{ inputs.target_branch }}" \
            --head "$BRANCH" \
          || echo "PR already exists, skipping creation"
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
